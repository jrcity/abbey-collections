rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow anyone to read (view products)
      allow read: if true;
      
      // Allow only logged-in users to write (add/edit products)
      allow write: if request.auth != null;
    }
  }
}